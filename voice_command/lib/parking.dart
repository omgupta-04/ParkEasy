import 'package:flutter/material.dart';

class ShowNearParking extends StatelessWidget {
  const ShowNearParking({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('THESE ARE THE NEARBY PARKING')),
    );
  }
}
